spring:
  application:
    name: my-clash-royale-server
  ai:
    openai:
      api-key: ${DEEPSEEK_API_KEY}
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
          temperature: 0.7

server:
  port: 8080
#  servlet:
#    context-path: /api

logging:
  level:
    com.example.myclashroyaleserver: DEBUG

